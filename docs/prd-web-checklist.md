# Checklist PRD Web Utama

## Tujuan

Dokumen ini menjadi tracker implementasi antara PRD aplikasi web utama dan status aktual `apps/web`.

Status dipakai dengan arti:

- `SUDAH`: requirement inti sudah hidup di web
- `PARSIAL`: fondasi sudah ada, tetapi alur belum lengkap
- `BELUM`: baru berupa rencana, shell kosong, atau belum muncul di web

## Ringkasan Cepat

| Area | Status | Catatan |
|---|---|---|
| Login tunggal | SUDAH | Auth hybrid + session cookie sudah hidup, redirect login/logout kini aman untuk host request nyata saat standalone/reverse proxy sehingga tidak lagi jatuh ke `0.0.0.0` pada smoke hosting |
| Dashboard utama | SUDAH | Pusat kendali ERP, KPI lintas domain, status sumber data, struktur divisi (5 cluster) + sub-divisi tampil jelas, dashboard operasional kini dikelompokkan per divisi besar dengan penanda sub-divisi aktif vs menunggu integrasi, KPI aktif sudah mencakup Penjualan, CS, NOC, Troubleshoots, Dismantle, Creator Digital, Billing, HR, dan Inventory, filter bulan/tahun/sub-divisi, alert silang domain dengan modul terdampak + dampak + langkah berikutnya, panel tindakan berikutnya, KPI proses dengan drilldown berbasis focus context lintas domain (support dan non-support) yang kini juga membawa periode dashboard dan untuk Sales/Billing/HR/Inventory makin selaras 1:1 dengan rule SQL template KPI, list kerja, approval, shortcut modul, dan audit aktivitas sudah ada; layout dua kolom dashboard juga sudah dirapatkan agar panel kanan tidak menyisakan gap bawah saat tinggi konten berbeda |
| KPI dashboard custom per manager divisi | PARSIAL | PRD kustomisasi KPI manager per divisi sudah tersedia di `docs/dashboard-kpi-customization-prd.md`, service backend definisi KPI custom + audit + validasi scope manager sudah tersedia, API CRUD sudah hidup di `/api/dashboard/kpi-definitions`, panel `Kelola KPI` sudah tampil di dashboard untuk manager/super admin, dashboard operasional sudah membaca KPI custom aktif (berdasarkan template) dan kini memiliki KPI baseline sistem (`SYSTEM`) yang bisa dioverride (termasuk menonaktifkan KPI default) pada scope divisi/sub-divisi, SUPER_ADMIN dapat memilih scope KPI lewat query parameter agar kartu operasional ikut ter-update, template KPI sudah mendukung PERCENTAGE (rasio) serta SUM (nominal) untuk beberapa skenario inti, panel manager kini memberi prefill aman untuk `metric type` + `drilldown` berdasarkan template, dan runtime kartu default sistem juga mulai memprioritaskan mapping template terbaru agar link lama baseline tidak tertinggal | Formula komposit lintas beberapa sumber yang lebih kaya, kontrol UX lanjutan (mis. drag-drop reorder), dan template SUM lain (contoh nominal payment/collection) masih belum lengkap | Sangat Tinggi |
| Keputusan fase awal divisi fondasi | SUDAH | Fase awal migrasi kini dikunci dengan prinsip `web-psb-perkasa` sebagai fondasi Divisi `Pemasaran dan Pelayanan`, sedangkan integrasi ke `Teknis dan Expan`, `General Affair`, `Finance dan HR`, dan `Operasional` diposisikan sebagai tahap berikutnya di dokumen organisasi, role target, readiness, dan PRD worklist | Dokumen implementasi lanjutan harus tetap konsisten dengan scope fase awal ini | Sangat Tinggi |
| List kerja terpadu lintas domain | PARSIAL | Dashboard sudah memiliki panel `List Kerja Terpadu` dan kontrak item kerja dasar, lalu PRD layar penuhnya kini didefinisikan di `docs/web-list-kerja-terpadu-prd.md` | Belum ada route modul penuh, filter global, tab queue per role, dan CTA kerja lintas domain yang terpusat | Sangat Tinggi |
| Spec implementasi `List Kerja Terpadu` | SUDAH | Spesifikasi implementasi route `/dashboard/worklist`, kontrak data `WorklistItem`, query parameter, tab queue, layout, integrasi dashboard, dan tahapan MVP kini terdokumentasi di `docs/web-list-kerja-terpadu-implementation-spec.md` agar siap diturunkan ke coding | Implementasi route, komponen, dan service masih berikutnya | Sangat Tinggi |
| UAT Divisi Pemasaran dan Pelayanan | SUDAH | Checklist UAT khusus fase awal kini tersedia di `docs/web-pemasaran-pelayanan-uat-checklist.md`, mencakup urutan uji, flow wajib, bukti lulus, dan aturan `pass/partial/fail` untuk role inti legacy | Hasil eksekusi UAT nyata per role masih harus diisi saat pilot berjalan | Sangat Tinggi |
| Daily Activity | SUDAH | Menu khusus plan pagi per divisi/sub-divisi dan level Manager/SPV/Leader, closing sore, approval manager, performa harian-mingguan-bulanan, kalender plan, dan export CSV sudah ada |
| Navigasi shell | SUDAH | Sidebar, topbar, RBAC menu sudah aktif; urutan menu kini diprioritaskan per role/divisi dan dipisah `Menu Utama` vs `Pengaturan` agar tampilan lebih fokus per role, sidebar kini juga bisa diminimalkan/ditampilkan kembali di desktop dan dibuka-tutup sebagai drawer di mobile, dan inventaris aktual menu/fitur/kolom per role kini terdokumentasi di `docs/web-role-division-menu-feature-catalog.md` |
| Import center | PARSIAL | Read-side kuat, pipeline inti hidup, dan upload ulang destruktif sudah dikunci; parser fleksibel dan revision flow formal belum penuh |
| Detail batch | SUDAH | Row review dan summary batch sudah ada |
| Sales | PARSIAL | Lead review, coverage, sales survey, sales order, work order, dan aktivasi subscription awal sudah bisa create; CTA row review kini juga bisa membawa prefill lead/order ke form target, workflow belum lengkap |
| Customers | PARSIAL | Customer master sudah bisa create, lifecycle belum lengkap |
| Support | PARSIAL | TT review sudah bisa create, update progress non-destruktif, prioritisasi follow-up, prioritisasi SLA due/overdue, lane ready close untuk ticket yang sudah matang ditutup, indikator reason/blocker close pada queue ticket, prioritas ticket otomatis berbasis queue reason, urutan section queue TT kini mengikuti urgensi tertinggi per lane, lane aktif TT kini dipisah lagi menjadi critical attention, planned follow up, dan waiting progress, ringkasan header TT kini menampilkan distribusi prioritas `P1-P4`, CTA rekomendasi per lane kini langsung diarahkan ke ticket teratas yang paling relevan dan membaca queue reason teratas agar label aksi lebih spesifik, tiap row TT kini juga menampilkan badge aksi disarankan dan helper langkah saat ini, urutan dan styling tombol aksi per row kini mengikuti aksi rekomendasi utama, aksi pendukung per row kini juga disaring sesuai queue reason agar opsi yang tampil lebih relevan, helper tambahan kini muncul saat aksi disederhanakan agar operator paham fokus operasional yang diprioritaskan, dan bahasa CTA lane/row kini disatukan agar kosakata aksi konsisten dari panel ringkas sampai detail ticket, eskalasi ticket non-destruktif dengan guard level SLA yang valid dan anti-duplikasi tanpa context baru, close dengan konteks progress terakhir dan guard backend agar ticket tidak bisa langsung ditutup tanpa progress aktif, kelola SLA dasar, tambah isolir aktif, restorasi isolir, dan dismantle history |
| Billing | PARSIAL | Generate invoice recurring single dan batch dari daftar billing-ready, generate invoice non-recurring one-time dengan nominal/deskripsi custom, pembatalan invoice unpaid, suspend/reconnect invoice single dan batch, collection action single dan batch dengan antrean batch berbasis jenis aksi serta guard eligibility invoice/action yang lebih ketat, collection follow-up queue yang kini dipisah recurring vs one-time, promise to pay queue dan suspend ready queue yang kini juga dipisah recurring vs one-time, histori collection action terbaru yang kini dipisah recurring vs one-time, reconnect ready queue dan write-off queue yang kini juga dipisah recurring vs one-time, segmentasi review recurring vs one-time invoice, auto-escalation read-side dari promise to pay overdue ke suspend ready, resolve follow-up collection yang kini ikut menyelaraskan status koleksi invoice secara aman termasuk penutupan formal write-off, reconnect ready queue yang hanya membaca invoice aktif pada jalur `RECONNECT`, serta payment entry dengan konteks tagihan, auto-close follow-up, dan auto-cleanup jalur suspend saat pembayaran mulai masuk sudah bisa create; invoice yang diaktifkan lagi dari suspend juga kembali keluar dari antrean reconnect ke follow-up normal, dan CTA row review kini bisa membawa prefill invoice maupun service number langsung ke form billing yang tepat |
| Inventory | PARSIAL | Review section operasional, item master, movement, ODP, port, device assignment, return perangkat, update status port awal, konteks operasional ODP/maps dasar, workflow request barang teknisi, alur barang pinjam-kembali inventory, dan jalur barang masuk yang lebih mudah untuk gudang sudah bisa create; CTA row review kini juga bisa membawa prefill request/loan ke form target |
| HR | PARSIAL | Review section operasional, employee master, attendance, loan, payroll awal, archive employee, reactivate employee non-destruktif, fondasi geofence/radius attendance web, fondasi face attendance web, capture kamera browser dasar, workflow review verifikasi wajah, scoring placeholder review wajah, confidence band, jalur auto-review aman, policy auto-verify berbasis threshold, analytics outcome verifikasi wajah untuk backlog/status final/quality sample, baseline referensi wajah per employee untuk fondasi matching engine, auto-suggest baseline dari capture VERIFIED terbaru, matching recommendation berbasis baseline aktif (`MATCH/REVIEW_MANUAL/RETAKE`), feedback loop reinforce baseline + antrean retake side-car operasional, baseline history dan scoring trend per employee, deteksi drift baseline (`STABLE/WATCHLIST/DRIFTING`), serta priority queue operasional untuk retake pending dan employee drifting sudah hidup; CTA row review kini juga bisa membawa prefill attendance/loan/employee/payroll ke form tindak lanjut, recognition engine wajah penuh masih berikutnya |
| Settings access | SUDAH | Permission master dinamis, role-permissions, dan audit perubahan permission sudah hidup di review DB |
| Settings users | SUDAH | List, create, edit, reset password, deactivate/reactivate, dan audit perubahan user sudah ada |
| Mobile readiness | PARSIAL | Shell adaptif, mode detail mobile belum merata |
| Prisma target | BELUM | Saat ini masih memakai `mysql2` langsung |

## Checklist Detail

| Requirement PRD | Status | Implementasi Saat Ini | Gap Utama | Prioritas |
|---|---|---|---|---|
| Login username/password tunggal | SUDAH | Halaman login, session cookie, logout, redirect per role | Belum ada reset password dan full auth admin | Tinggi |
| Role user `SUPER_ADMIN`, `ADMIN_DIVISI`, `OPERATOR` | SUDAH | RBAC route, sidebar, shortcut, action matrix | Permission sudah bisa dikelola via settings/access saat review DB aktif, namun audit lintas seluruh write action belum terpusat | Tinggi |
| Dashboard KPI lintas domain | SUDAH | Dashboard kini berfungsi sebagai pusat kendali ERP dengan lapisan `Pusat Kendali ERP`, `Dashboard Operasional`, `KPI Proses`, `Alert Silang Domain`, `Tindakan Berikutnya`, `Kontrol Lintas Domain`, `List Kerja`, `Approval`, `Shortcut Modul`, dan `Audit`, ditambah kartu `Penjualan`, `CS`, `NOC`, dan `Creator Digital` yang menarik metrik review DB secara aman; setiap alert silang domain kini juga menjelaskan modul terdampak, dampak lintas domain, dan langkah berikutnya, lalu diringkas lagi menjadi CTA prioritas dari alert, worklist, dan queue role aktif, KPI proses support bisa membuka lane dengan focus context seperti `SLA_OVERDUE`, `OVERDUE_RATE`, dan `OPEN_TICKETS`, dan focus drilldown kini juga aktif untuk modul non-support (Sales/Billing/HR/Inventory) dengan filter level baris berbasis period/status/tag internal; filter sudah query-driven di service layer untuk period/status utama, untuk focus penting seperti order bulanan sales, order/survey digital, aktivasi bulanan, overdue/partial/suspend billing, attendance/pinjaman HR, serta request pending/mutasi inventory basis SQL kini makin mengikuti rule template KPI yang sama, KPI komposit seperti `ACTIVATION_RATE` serta `ATTENDANCE_RATE` menampilkan summary agregat berbasis query penuh untuk pembilang-penyebutnya, `BILLING_OVERDUE_AMOUNT` menampilkan antrean berdasarkan outstanding terbesar agar nominal overdue tidak tercampur dengan overdue count biasa, section drilldown komposit menampilkan summary agregat langsung di header, dan kini baik `KPI Proses` maupun `Dashboard Operasional` menampilkan hint komposit yang dipoles dalam bentuk mini badge + teks ringkas agar angka komposit bisa dibaca lebih cepat sebelum drilldown dibuka | Perluasan query-driven langsung ke section lain yang belum dipresisikan penuh, serta sinkronisasi template KPI komposit lintas sumber yang lebih luas masih perlu diperdalam | Tinggi |
| Dokumentasi perspektif role/divisi terhadap menu, fitur, dan kolom web | SUDAH | Inventaris aktual role aktif, divisi, sidebar, capability, katalog menu global, katalog kolom per menu, dan detail perspektif tiap role sudah dirangkum di `docs/web-role-division-menu-feature-catalog.md` agar PRD bisa dibaca langsung dari sudut pandang operasional | Dokumen ini masih perlu terus dijaga sinkron setiap kali ada menu baru, kolom baru, atau perubahan permission | Tinggi |
| Dokumentasi readiness cutover per role/divisi | SUDAH | Matriks readiness yang memutuskan status `GO/PILOT/PARTIAL/NO-GO`, blocker, dan gelombang cutover per role aktif kini dirangkum di `docs/web-role-cutover-readiness.md` agar PRD tidak berhenti di level inventaris menu saja | Status readiness perlu diperbarui setiap kali flow baru, queue baru, atau guard capability role berubah | Tinggi |
| PRD modul `List Kerja Terpadu` | SUDAH | Route target `/dashboard/worklist`, target role, filter global, tab queue per role, struktur item, kolom tabel, panel detail, CTA contextual prefill, dan rollout bertahap kini terdokumentasi di `docs/web-list-kerja-terpadu-prd.md` | Implementasi route, query, dan queue lintas domain masih berikutnya | Sangat Tinggi |
| Daily activity plan pagi dan closing sore | SUDAH | Menu `Daily Activity` di `/dashboard/daily-activity`, tabel `daily_activity_items`, form plan pagi per divisi/sub-divisi dan level `Manager/SPV/Leader`, form closing sore, approval manager (approve/reject) per divisi/sub-divisi, status `PLANNED/DONE/PENDING`, alasan pending, aksi lanjut, performa harian-mingguan-bulanan berbasis approval, kalender plan bulanan, dan export CSV sudah hidup | Belum ada export XLSX/PDF, rekap KPI formal lintas periode, serta approval multi-level (SPV -> Manager) bila dibutuhkan | Tinggi |
| Sidebar domain + topbar context | SUDAH | Shell aplikasi, active state, navigasi antardomain langsung di header tiap menu, serta landing operasional domain yang menonjolkan alur menu dan integrasi lintas modul sudah ada | Quick search modul belum nyata | Sedang |
| Pusat import sebagai modul kelas satu | PARSIAL | Halaman daftar batch, detail batch, create batch, upload file sumber, parser ke row staging, validasi, trigger transform, histori aksi batch, serta guard non-destruktif agar batch yang sudah berisi staging tidak bisa di-upload ulang sudah hidup | Parser format bebas per domain dan revision flow formal per batch belum aktif penuh | Sangat Tinggi |
| Pipeline transform tahap 1-4 | PARSIAL | Tombol eksekusi transform tahap 1-4 dari web sudah ada + histori transform per batch | Scope per batch sudah ditambah, namun perlu audit menyeluruh setiap statement stage agar semua domain 100% aman dan idempotent | Sangat Tinggi |
| Filter review batch dan row bermasalah | SUDAH | Detail row sudah ada + filter interaktif (status, domain, pencarian) | - | Sedang |
| Sales shell: lead, coverage, survey, order | PARSIAL | Lead review, coverage create, survey create, sales order create, work order create, aktivasi subscription, review coverage/survey/order/work order/subscription, serta landing menu yang kini menampilkan alur utama domain dan integrasi langsung ke customer/inventory/billing sudah ada; panel `Aksi Sales Prioritas` kini menghubungkan section review ke form pipeline sales yang relevan untuk role aktif, kartu review generiknya juga sudah menampilkan CTA per-section langsung di header, dan row review kini bisa menampilkan CTA yang lebih spesifik seperti tindak lead, coverage, order, atau aktivasi sekaligus membawa prefill `lead`/`order` ke form target | Workflow corporate lanjut, quotation/contract, dan otomasi pasca-aktivasi belum hidup | Sangat Tinggi |
| Customer shell: master, address, subscription, histori | PARSIAL | Create customer + primary address, serta landing menu yang kini menonjolkan alur customer master -> address -> subscription dan integrasi ke sales/support/billing sudah ada | Subscription lifecycle dan histori layanan belum lengkap | Tinggi |
| Support shell: TT, isolir, dismantle, SLA | PARSIAL | TT open review sudah ada, create TT, update progress non-destruktif dengan PIC/follow-up side-car, prioritas queue berbasis follow-up overdue/terdekat, prioritas queue berbasis SLA due/overdue, lane `ready close` untuk ticket yang sudah punya progress valid tanpa follow-up aktif dan tanpa eskalasi yang lebih baru, indikator `queue reason`, `queue priority`, dan `close candidate` pada panel TT agar operator cepat membaca blocker close dan urutan urgensi, urutan section panel TT kini mengikuti prioritas tertinggi yang benar-benar ada pada tiap lane sehingga ticket `P1/P2` tampil duluan, lane aktif ticket kini dipisah lagi menjadi `critical attention`, `planned follow up`, dan `waiting progress`, ringkasan header panel TT juga menampilkan distribusi `P1-P4`, lane kosong otomatis disembunyikan agar fokus operator tetap rapat, CTA rekomendasi per lane kini langsung melompat ke aksi paling relevan untuk ticket teratas seperti `update progress`, `eskalasi`, `close`, atau `cek SLA` dengan label yang membaca `queue reason` teratas seperti `ESCALATION_PENDING`, `FOLLOW_UP_OVERDUE`, `SLA_DUE_TODAY`, `FOLLOW_UP_SCHEDULED`, atau `WAITING_PROGRESS`, setiap row TT kini juga menampilkan badge `Aksi Disarankan` dan helper `Langkah saat ini`, tombol aksi per row kini diurutkan dan diberi styling utama sesuai rekomendasi tindakan terdepan, aksi pendukungnya ikut disaring agar ticket `ready close`, `escalation pending`, `sla overdue`, `follow-up overdue`, dan konteks lain tidak dipenuhi opsi yang kurang relevan, helper tambahan kini menjelaskan saat aksi memang disederhanakan, bahasa CTA header lane kini memakai kamus label yang sama dengan CTA row agar prioritas aksi konsisten dari panel ringkas sampai detail ticket, serta landing menu support kini menampilkan alur utama domain dan integrasi langsung ke customer/billing/inventory, eskalasi ticket non-destruktif dengan side-car log, guard backend agar level `OVERDUE/DUE_TODAY` hanya dipakai saat SLA ticket memang sesuai, serta anti-duplikasi bila belum ada progress baru sesudah eskalasi terakhir, close ticket dengan snapshot progress terakhir dan guard backend yang mewajibkan fase `ON_PROGRESS/FOLLOW_UP` sebelum close, kelola SLA dasar, tambah isolir aktif, restorasi isolir, dan dismantle flow dasar sudah ada; tampilan form dan tombol aksi kini mengikuti capability role (aksi yang tidak punya permission otomatis tersembunyi dan lane bisa tampil sebagai mode baca saja) | SLA penuh dan otomasi workflow support lanjutan belum ada | Sangat Tinggi |
| Inventory shell: item, stock movement, ODP, device assignment | PARSIAL | Review item inventory, review stock movement, review ODP/port, review device assignment, review port bermasalah, review device return, review request inventory teknisi, review pinjaman inventory, create item master, create stock movement, create ODP, assign port, update status port, device assignment, return perangkat, panel operasional ODP/maps awal, request barang teknisi, update status request sampai selesai, tagging sub-divisi teknisi pada request inventory, panel antrean request per sub-divisi/status, pembuatan pinjaman barang, proses pengembalian sebagian/penuh, pemulihan stok otomatis saat barang kembali, form/panel khusus barang masuk yang lebih mudah untuk gudang, serta landing menu yang kini menonjolkan alur stok/ODP/request dan integrasi ke sales/support/HR sudah ada; panel `Aksi Inventory Prioritas` kini menghubungkan review request/stok/ODP ke form yang relevan untuk gudang atau teknisi, kartu review generiknya juga sudah menampilkan CTA per-section langsung di header, dan row review kini bisa menampilkan CTA yang lebih spesifik seperti proses request, return perangkat, pengembalian pinjaman, atau atur port sekaligus membawa prefill `request`/`loan` ke form target | Device return lanjutan (return sebagian/serial per perangkat), port reserve per order, accessories kit yang lebih detail, integrasi assignment penuh ke work order, dan alur request multi-item seperti cart/checkout masih perlu diperdalam | Tinggi |
| HR shell: employee, attendance, salary, loan | PARSIAL | Review employee, attendance, loan, salary slip, create employee master, archive/reactivate employee non-destruktif, create attendance, create loan, create payroll awal, release payroll, void payroll, update status loan, void loan, penguatan suggestion/prefill aman untuk correction attendance, update/void loan, release/void payroll, guard backend agar slip payroll yang sudah `VOIDED` tidak bisa dirilis kembali, konfigurasi geofence attendance, capture lokasi browser saat check-in, validasi radius opsional/wajib, log lokasi attendance, konfigurasi face attendance, referensi verifikasi wajah/manual review saat check-in, capture kamera browser dasar yang mengisi `faceCaptureRef` otomatis, workflow review operasional wajah dengan status `PENDING_REVIEW`/`VERIFIED`/`REJECTED`, scoring placeholder dengan rekomendasi keputusan dan alasan review, confidence band `HIGH/MEDIUM/LOW`, tombol `Auto-Verify Aman`, policy admin untuk toggle auto-verify dan minimum score threshold, serta landing menu yang kini menonjolkan alur employee/attendance/payroll dan integrasi ke inventory/daily activity/settings users sudah ada; panel `Aksi HR Prioritas` kini menghubungkan review employee/attendance/payroll/loan ke form tindak lanjut yang relevan untuk role aktif, kartu review generiknya juga sudah menampilkan CTA per-section langsung di header, dan row review kini bisa menampilkan CTA yang lebih spesifik seperti review wajah, koreksi attendance, update loan, atau rilis payroll sekaligus membawa prefill `attendance`/`loan`/`employee`/`payroll` ke form target | Recognition engine wajah penuh dan perhitungan otomatis lebih detail belum hidup | Tinggi |
| Billing shell: invoice, payment, collection, overdue | PARSIAL | Generate invoice recurring dari subscription aktif, batch recurring invoice dari daftar billing-ready, generate invoice one-time `INSTALLATION/ADJUSTMENT/TERMINATION` dengan nominal dan deskripsi custom, pembatalan invoice unpaid, suspend/reconnect invoice dari form status secara single dan batch, collection action single dan batch dari antrean yang menyesuaikan jenis aksi dengan guard invoice layak tindak dan pembatasan action strategis ke status `OPEN`, collection follow-up queue dengan state prioritas yang kini dipisah untuk `recurring` dan `one-time`, `promise to pay queue` dan `suspend ready queue` yang kini juga dipisah untuk `recurring` dan `one-time`, histori `collection action terbaru`, `reconnect ready queue`, dan `write off queue` yang kini juga dipisah untuk `recurring` dan `one-time`, segmentasi review `recurring` vs `one-time` invoice untuk lane tindak lanjut dan invoice terbaru, auto-escalation read-side dari janji bayar overdue ke `suspend ready`, resolve follow-up collection yang kini menormalkan kembali janji bayar selesai/batal, menutup write-off yang selesai ke `CLOSED`, dan mencabut sinyal suspend saat action suspend dibatalkan, antrean umum billing yang kini hanya memuat invoice collectible, form status invoice dengan konteks reconnect aman, payment entry dengan konteks remaining/follow-up/action terakhir, auto-close follow-up OPEN, auto-cleanup jalur suspend saat pembayaran mulai masuk, aktivasi ulang invoice suspend yang otomatis mengeluarkan invoice dari jalur reconnect dan menutup action reconnect terbuka, serta landing menu yang kini menonjolkan alur invoice/payment/collection dan integrasi ke customer/support/sales sudah ada; tampilan form write-side kini mengikuti capability role (create/update) dan role read-only melihat banner `Mode baca saja`, sementara panel `Aksi Billing Prioritas` menghubungkan section review ke form billing yang memang tersedia untuk role aktif, kartu review generiknya juga sudah menampilkan CTA per-section langsung di header, dan row review kini bisa menampilkan CTA yang lebih spesifik seperti reconnect, suspend, resolve follow up, atau tindak collection sekaligus membawa prefill `invoice` maupun `service` ke form target | Lifecycle invoice penuh, otomasi billing lanjutan, dan penyesuaian non-recurring yang lebih kaya masih belum lengkap | Sangat Tinggi |
| Settings access dan permission | SUDAH | Permission master dinamis + audit permission + set role-permissions sudah tersedia di `settings/access` | - | Tinggi |
| Settings users internal | SUDAH | Direktori user, create user, edit profil, reset password, deactivate/reactivate, dan panel audit perubahan sudah ada | Audit masih terfokus pada user internal, belum lintas seluruh modul write action | Tinggi |
| Audit log dasar | PARSIAL | Audit perubahan import batch, user internal, permission, role-permission, write action support utama, write action inventory utama, write action billing utama, write action sales utama, serta create action HR utama, archive/reactivate employee HR, correction attendance HR, konfigurasi geofence attendance HR, konfigurasi face attendance HR, review verifikasi wajah HR, update status loan HR, void loan HR, release slip gaji HR, dan void slip gaji HR sudah bisa dibaca terpusat di dashboard | Delete/approval HR lanjutan dan modul lain belum tercakup secara menyeluruh | Tinggi |
| Kompatibilitas mobile web | PARSIAL | Shell dan login cukup aman di layar kecil | Tabel review/detail belum punya pola card/stacked merata | Tinggi |
| Android wrapper readiness | PARSIAL | Arsitektur mengarah ke sana dan shell aman sebagai basis | Integrasi wrapper dan pengujian jalur mobile belum formal | Sedang |
| Prisma sebagai target data layer | BELUM | Service layer sudah ada | Implementasi masih memakai `mysql2/promise` langsung | Sedang |

## Evidence Implementasi

### Auth

- `apps/web/app/(auth)/login/page.tsx`
- `apps/web/app/api/auth/login/route.ts`
- `apps/web/app/api/auth/logout/route.ts`
- `apps/web/lib/auth.ts`
- `apps/web/lib/auth-session.ts`
- `apps/web/lib/access-control.ts`

### Dashboard

- `apps/web/app/(app)/dashboard/page.tsx`
- `apps/web/lib/services/dashboard-service.ts`
- `apps/web/components/data-source-status.tsx`
- `apps/web/components/dashboard/operational-division-board.tsx`
- `apps/web/components/dashboard/worklist-board.tsx`
- `docs/web-list-kerja-terpadu-prd.md`

### Organisasi dan Role

- `docs/org-division-baseline.md`
- `docs/web-psb-target-role-design.md`
- `docs/web-role-cutover-readiness.md`
- `docs/web-pemasaran-pelayanan-uat-checklist.md`
- `docs/web-list-kerja-terpadu-implementation-spec.md`

### Import

- `apps/web/app/import/page.tsx`
- `apps/web/app/import/[batchId]/page.tsx`
- `apps/web/lib/services/import-service.ts`
- `apps/web/app/api/import/batches/route.ts`
- `apps/web/app/api/import/batches/[id]/route.ts`

### Domain

- `apps/web/app/[domain]/page.tsx`
- `apps/web/components/domain-shell.tsx`
- `apps/web/lib/services/domain-service.ts`
- `apps/web/lib/mock-domains.ts`
- `docs/web-role-division-menu-feature-catalog.md`
- `docs/web-role-cutover-readiness.md`

### Settings

- `apps/web/app/settings/access/page.tsx`
- `apps/web/app/settings/users/page.tsx`
- `apps/web/components/auth-user-create-form.tsx`
- `apps/web/app/api/settings/users/route.ts`
- `apps/web/lib/services/auth-user-service.ts`

## Prioritas Implementasi Berikutnya

1. Hidupkan inventory dan HR:
   review section operasional + write action awal.
2. Rapikan mobile behavior:
   pola card/stacked row untuk tabel sempit dan form panjang.
3. Perluas audit perubahan lintas modul:
   bawa pola audit formal dari `settings/users` ke import, sales, support, dan billing.
4. Perluas parser legacy per domain:
   dukung format input yang lebih longgar di luar JSON/XLSX terstruktur.

## Catatan

- Dokumen ini adalah checklist implementasi web, bukan pengganti PRD.
- Status dapat berubah cepat seiring iterasi fitur, jadi file ini sebaiknya diperbarui setiap kali milestone web bertambah.
